export async function POST(request: Request) {
    //send this the objet result of any scraper and it will search for the pc parts in the database and return the result



// {
//     cpu_id: cpuBrand && cpuSpecs.Series && (await getProduct(cpuSpecs.Series, cpuBrand))?.id,
//       gpu_chipset_id: gpuSpecs && gpuSpecs["Base Graphics Model"] && (await prisma.gpuChipset.findUnique({where: {name: cleanTrademarks(gpuSpecs["Base Graphics Model"])}}))?.id,
// }
}