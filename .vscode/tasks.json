{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "prisma generate",
            "type": "shell",
            "command": "npx prisma format; npx prisma generate; npx prisma db push",
            "group": "build",
            "presentation": {
                "reveal": "always"
            },
            "problemMatcher": "$eslint-stylish"
        }
    ]
}